# 算法
> 算法是解决特定问题求解步的描述，在计算机中表现为指令的有限序列，并且每条指令表示一个或多个操作

## 算法、算法的特征、算法的要求、算法的效率

## 时间复杂度、空间复杂度
	- 常见时间复杂度耗时：O(1) < O(log n) < O(n) < O(nlog n) < 0(n2) < O(n^3) < O(2^n ) < O(n!) < O(n^n)
	- 空间复杂度为O(1)指算法原地工作